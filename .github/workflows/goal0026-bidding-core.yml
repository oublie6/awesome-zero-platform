name: Goal 0026 Bidding Core Verification

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  contents: read
  statuses: write

concurrency:
  group: goal0026-bidding-core-verification
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Resolve latest main and publish pending status
        id: main
        uses: actions/github-script@v7
        with:
          script: |
            const ref = await github.rest.git.getRef({ owner: context.repo.owner, repo: context.repo.repo, ref: 'heads/main' });
            const sha = ref.data.object.sha;
            core.setOutput('sha', sha);
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner, repo: context.repo.repo, sha,
              state: 'pending', context: 'goal0026/bidding-core',
              description: 'Goal 0026 bidding-core verification is running',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });
      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.main.outputs.sha }}
      - uses: actions/setup-go@v5
        with:
          go-version: 1.25.8
          cache-dependency-path: server/go.sum
      - name: Verify formatting
        id: formatting
        run: |
          files=$(gofmt -l server/business/doudizhu/domain/bidding)
          test -z "$files" || { printf '%s\n' "$files"; exit 1; }
      - name: Ordinary tests
        working-directory: server
        run: go test -count=1 -p 1 -parallel 1 ./business/doudizhu/domain/bidding
      - name: Race tests
        working-directory: server
        run: go test -race -count=1 -p 1 -parallel 1 ./business/doudizhu/domain/bidding
      - name: Vet
        working-directory: server
        run: go vet ./business/doudizhu/domain/bidding
      - name: Publish result
        if: always()
        uses: actions/github-script@v7
        env:
          SHA: ${{ steps.main.outputs.sha }}
          FORMAT: ${{ steps.formatting.outcome }}
          JOB: ${{ job.status }}
        with:
          script: |
            const success = process.env.JOB === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner, repo: context.repo.repo, sha: process.env.SHA,
              state: success ? 'success' : 'failure', context: 'goal0026/bidding-core',
              description: success ? 'Goal 0026 bidding core passed' : 'Goal 0026 bidding core failed',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });
